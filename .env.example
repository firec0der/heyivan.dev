# Docker Compose variable defaults.
# Copy to .env and edit for local overrides — .env is gitignored.

# Host ports
WEB_PORT=3344
STORYBOOK_PORT=3345

# Set to true on macOS if HMR feels sluggish (VirtioFS fallback).
WATCHPACK_POLLING=false
CHOKIDAR_USEPOLLING=false
